tabulate~=0.10.0
asyncpg~=0.31.0
certifi~=2026.5.20
pyyaml~=6.0.3
python-dotenv~=1.2.2
colorama==0.4.6
iniconfig==2.3.0
packaging==25.0
pluggy==1.6.0
Pygments==2.20.0
pytest>=9.0.3
pytest-asyncio==1.3.0
ruff==0.14.4
discord.py>=2.7.1
motor~=3.7.1
pymongo>=4.4.0,<5.0.0

# Shared engines (storage_engine + admin_engine) fetched from GitHub at build time.
# Pin to a release tag once EmpireSystems cuts one; tracks the deploy branch (dev) for now.
EmpireSystems[admin,discord] @ git+https://github.com/Empire-of-Shadows/EmpireSystems.git@dev
